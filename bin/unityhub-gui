#!/bin/bash
# Opens Unity Hub on the virtual desktop, only to activate the Personal license.
# Also the handler for unityhub:// links (the sign-in callback). The license
# (~/.local/share/unity3d/Unity/Unity_lic.ulf) lives in the home volume.
export DISPLAY="${DISPLAY:-:1}"
exec unityhub --no-sandbox --disable-gpu "$@"
